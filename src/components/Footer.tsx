import { Layout } from 'antd';
import * as React from 'react';
const { Footer } = Layout;
export default () => (
  <Footer style={{ textAlign: 'center' }}>
    A React starter with ant-design and typescript
  </Footer>
);
